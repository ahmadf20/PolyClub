enum ButtonType { save, next }

/// - `all` is a list of all rooms
/// - `mine` is a list of all rooms created by the user
/// - `recommendation` is a list of recommended rooms based on his chosen topics
/// - `start_time` is a list of scheduled rooms
enum RoomListType { all, mine, recommendation, start_time }

enum PeopleListType { search, following, follower }
